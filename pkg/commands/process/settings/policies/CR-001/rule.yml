name: "Do not send sensitive data to loggers."
query: |
  policy_failure = data.bearer.leakage.policy_failure
modules:
  - path: policies/shared/common.rego
    name: bearer.common
  - path: policies/CR-001/leakage.rego
    name: bearer.leakage